//
//  Constants.swift
//  NLPubMed Assistant
//
//  Created by Octa Marina on 18.04.2024.
//

import Foundation

struct Constants{
    struct Sagues{
        static let registerIdentifier = "registerIdentifier"
        static let loginIdentifier = "loginIdentifier"
        static let autoLoginIdentifier = "autoLoginIdentifier"
        static let openWebviewIdentifier = "openWebviewIdentifier"
    }
    
    struct Animations{
        static let animationDuration = 0.3
    }
    
    struct Networking{
        static let baseURL = "http://localhost:8000/ai_processing"
    }
}
